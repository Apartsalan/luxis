# Audit workspace — contains DB snapshots + screenshots with REAL client data. NEVER commit.
.audit/
# Playwright MCP scratch (snapshots/console logs may contain real data)
.playwright-mcp/

# Generated dep exports (derived from backend/uv.lock at build/CI time — never commit)
backend/requirements.txt
backend/requirements-dev.txt

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
*.egg

# Node.js
node_modules/
.next/
out/
*.tsbuildinfo

# Environment
.env
.env.local
.env.production
.env.bak*
*.env.bak*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
*.log

# Generated documents
generated_docs/

# Coverage
htmlcov/
.coverage
coverage.xml

# mypy
.mypy_cache/

# ruff
.ruff_cache/

# pytest
.pytest_cache/

# Playwright
frontend/e2e/.auth/
frontend/test-results/
frontend/playwright-report/
.playwright-mcp/
test-results/
playwright-report/
frontend/e2e/.auth/
*.png
*.jpeg

# Test/preview artifacts (S145)
*.eml
preview_*.html
luxis_template.html
reactie_verweer.html

# BaseNet-export (S166/S167) — bevat ECHTE cliënt-PII. NOOIT committen.
# Xml_*.zip = metadata-export; 160*.zip = documenten-backup (~8GB, S167).
# Blanket *.zip: er hoort geen enkele zip in deze repo getrackt te worden.
*.zip
*.base.*.xml
basenet-export/

# Leesbare API-sleutels — nooit meecommitten (akkoord Arsalan S193)
.codex/

# Parallel Codex-spoor + gevoelige bron-bestanden — bewust NIET in de repo
# (S200-afspraak: Arsalan beslist). Per ongeluk mee-gecommit in eda3f2b (S203),
# daarna weer verwijderd. Blijven lokaal op schijf staan.
.agents/
.claude/scheduled_tasks.lock
tmp_*.sql
templates/.s201-prod-match.sql
# Bank-afschrift derdengelden + algemene voorwaarden (PDF) — gevoelig
CSV_A_*.csv
Algemene-voorwaarden-*.pdf
Terms*.pdf
Terms-and-conditions-*.pdf
